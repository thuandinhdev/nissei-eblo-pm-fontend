import {StringToArrayFilterPipe} from './string-to-array-filter.pipe';

describe('StringToArrayFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new StringToArrayFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
