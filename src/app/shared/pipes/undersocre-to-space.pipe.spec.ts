import {UndersocreToSpacePipe} from './undersocre-to-space.pipe';

describe('UndersocreToSpacePipe', () => {
    it('create an instance', () => {
        const pipe = new UndersocreToSpacePipe();
        expect(pipe).toBeTruthy();
    });
});
