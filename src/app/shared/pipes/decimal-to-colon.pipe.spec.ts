import {DecimalToColonPipe} from './decimal-to-colon.pipe';

describe('DecimalToColonPipe', () => {
    it('create an instance', () => {
        const pipe = new DecimalToColonPipe();
        expect(pipe).toBeTruthy();
    });
});
