import {ObjToArPipe} from './obj-to-ar.pipe';

describe('ObjToArPipe', () => {
    it('create an instance', () => {
        const pipe = new ObjToArPipe();
        expect(pipe).toBeTruthy();
    });
});
