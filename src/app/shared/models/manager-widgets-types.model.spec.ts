import {ManagerWidgetsTypes} from './manager-widgets-types.model';

describe('ManagerWidgetsTypes', () => {
    it('should create an instance', () => {
        expect(new ManagerWidgetsTypes()).toBeTruthy();
    });
});
