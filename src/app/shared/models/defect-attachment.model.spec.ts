import {DefectAttachment} from './defect-attachment.model';

describe('DefectAttachment', () => {
    it('should create an instance', () => {
        expect(new DefectAttachment()).toBeTruthy();
    });
});
