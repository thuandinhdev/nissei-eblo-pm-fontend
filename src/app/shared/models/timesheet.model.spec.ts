import {Timesheet

.
Model;
}
from;
'./timesheet.model';

describe('Timesheet.Model', () => {
    it('should create an instance', () => {
        expect(new Timesheet.Model()).toBeTruthy();
    });
});
